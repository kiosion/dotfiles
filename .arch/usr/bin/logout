#!/bin/sh

# save state
~/.dotfiles/scripts/session_save.fish || true

# kill clipmenu
pkill -15 clipmenud

# quit bspwm
bspc quit

# quit xorg and xserver
#pkill -15 Xorg
#pkill -15 Xserver

#kill -SIGUSR1 $PPID
