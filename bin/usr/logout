#!/bin/sh

# quit bspwm
bspc quit

# quit xorg and xserver
pkill -15 Xorg
pkill -15 Xserver
pkill -15 Xsel

# kill some stuff i might leave open
pkill -15 clipmenud
pkill -15 discord
pkill -15 discord-canary
pkill -15 screencloud
pkill -15 nextcloud

# switch to TTY1 login
exit
